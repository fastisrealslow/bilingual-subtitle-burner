# Local code/cover regression only; production ASR/Ollama/media tools are separate.
pytest==8.4.2
Pillow==11.3.0
requests==2.32.5
jieba==0.42.1
fonttools==4.60.2
numpy==1.26.4
# Binary wheel available on Intel macOS as well as Linux; no local OpenCV build.
opencv-python-headless==4.10.0.84
# Bundled FFmpeg makes local media regressions runnable without a system install.
imageio-ffmpeg==0.6.0
