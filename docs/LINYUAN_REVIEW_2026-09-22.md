# 9 月 22 日续验记录

候选分支尚未达到“30%且质量明确优于主线”的替换标准，不应部署。

## 网页播放

- 对比页统一使用按需加载播放器。切换时释放上一条的视频资源，保留续播位置；失败会显示原因，并提供单独打开、下载入口。
- 新增只监听 `127.0.0.1` 的预览服务，支持 HTTP Range 和拖动进度条。HTML 与视频要保留完整目录；单独下载 HTML 不包含视频。
- 打开方式：`python scripts/open_linyuan_review.py`。
- 本机 Chrome 实测 125 个不同视频：全部开始播放并成功跳转至中间位置；同一时刻只保留一个视频资源。包含最新 18 条、主线、历史实验与参考。没有 JavaScript 错误，390px 手机宽度未溢出。
- 证据：`output/benchmark-20260921/page-check/playback-http-all.json`。这是浏览器播放兼容性验证，不是全片内容听看，也没有证明用户原先失败的具体原因。

## 成片率与失败原因

最近完整固定 100 为 e93fa87 / [35618921193](https://github.com/fastisrealslow/bilingual-subtitle-burner/actions/runs/35618921193)：18 自动通过、78 拒绝、4 未确定。88 原字幕漏检，不能把 18% 写为编辑合格率。主线同批自动出片 11%。

新增跨帧中央来源字幕检查已用 88 实际 MP4 验证拒绝；17 个源画面窗口 OCR 中，其余 16 个未新增标记，不代表内容合格。原生版式 99 无独立源窗口，此检查不适用。

9 条素材因选段边界规则没有候选：1、26、30、38、42、54、55、67、74。已下载原始转写和诊断，不以重复请求或放松门槛填数。

67 股东大会存在明确交接话筒和主持人致谢。新增通用交接识别，恢复原声 14.00–242.44 秒连续范围；含下一位说话人的混合字幕句整体留在范围外，没有猜测句内时间。保持转写不变，后续门禁继续执行。

[35677886473 实片复测](https://github.com/fastisrealslow/bilingual-subtitle-burner/actions/runs/35677886473)已完成：选段恢复，但现场开头为远景，逐帧取景连续 2 秒没有人脸，未出片。因此本次不增加成功数量。证据位于 `output/benchmark-20260921/handover-35677886473`。

另核查 5、15、47 的实际取景失败画面：分别有下方人物被原包装遮挡、近景贴近下方原字幕栏、九宫格及大标题覆盖等问题。现有证据不足以通过降低人脸或字幕门槛恢复，应继续寻找更干净的来源。

## 标题实验的有效性

独立 Actions 仍采用同一字幕的 8B 原提示词 / 8B 短提示词 / 14B 短提示词，样本为最新实片 66、95、99。阅读与独立核对保持原流程，短提示词去除外部事实示例，保留对象、条件、原话依据和封面完整性约束。

首轮 35677258087 的短提示词接口接错了消息结构，写稿调用没有执行；其摘句回退不能算短提示词或大模型改善，所有该实验分支无效。已增加真实消息格式的回归测试，并记录 `draft_interventions` / `experiment_valid`。3099aa7 修复后重新运行，未拿旧结果充数。

66 的第二 ASR 已完成：[35676361154](https://github.com/fastisrealslow/bilingual-subtitle-burner/actions/runs/35676361154)。它仍识别成“折远于长远”，没有提供足以纠正该词的新证据；字幕未改。标题应优先选择同段清楚完整的生意判断，不沿用“不是便宜”等原文没有的反差。

## 验证与限制

- 网页 Range、私有路径拒绝、消息适配、原始 ASR 范围都有回归覆盖。
- eee1782、3099aa7、fdf397d 三次 Linux 完整回归均成功；最新对应 [35677890499](https://github.com/fastisrealslow/bilingual-subtitle-burner/actions/runs/35677890499)。
- 媒体、固定批次、后续定向尝试分别计数。没有合并多轮最好结果，也未发布或替换线上。

## 后续真实结果与修复

- 有效标题对照 35678586927 的9组全部返回。8B原提示、8B短提示、14B短提示都存在语义或封面问题，不能宣布14B胜出。完整摘要见 `title-sep22-results.json`。
- 真实66的“买卖/生意”被分词器标成动词，导致自然标题进不了独立复核。修复后35679454645的14B选择了“一个买卖能长期做下去，才是好买卖”，并拒绝另外两条添加推断的候选。8B仍有错误，不能将一个例子当整体成功。
- 真实17讲光伏，旧提示连续写白酒和AI，来自提示中的参考事实。已移除这类示例，保留表达方式。6c8b83b复验不再串到白酒/AI，但因确定性说话人排除延续过长，标题拿不到光伏对象，仍未成片。后续只对明确问句后的同对象回答起手解除排除，仍须模型判断归属；不直接认定为嘉宾。
- 17与72原选段混入主持人尾巴。按原始时间戳去掉，17保留20.88秒；72该段不足20秒，拒绝而不补长。
- 95中两种模型均漏掉研究公司范围，新增逐项范围检查。第一条重放能挡住错误，但模型尚未写出合格替代，不能把拒绝算质量与产出同时提升。
- 封面先去除重复姓名，目录式文案、残句可采用完整标题的连续子句组合，再交独立复核。允许12至52字正文，单个判断不为22字下限凑理由。
- 18条园园完整视频约49分钟，35679265103全部完成固定Whisper模型转写，已阅读全文并逐条写内容观察，未声称人工逐句听音。涵盖具体取舍、经历、同话题追问、生活话题、多人归属与长访谈。仅机器识别的疑词不据此改字幕。
- 网页新增9组标题实测与18条参考内容拆解。Chrome 390px检查无溢出、无JS错误；视频播放逻辑不变。最近完整Linux CI：c7dc13a / 35680232155成功。

上述定向实验不修改固定100的18/78/4历史结果，不增加自动成片数量。目标30%与整批编辑验收仍未达成。
