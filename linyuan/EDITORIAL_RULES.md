# 日常完整观点标准（2026-09-06）

用户已隐藏上一批16～31秒的六条，要求重新生产并发布六条，以2～3分钟为主，可更长，拒绝十几秒短片。

- 日常选段与投稿共用 `editorial_policy.py`，版本2026090604，实际MP4下限120秒，主目标180秒；不机械裁掉必要解释。
- 每条保留一个连续论述：开头独立可懂，观点、理由或案例、结尾齐全。不能拼接无关短句、补空白、变速或重复来凑时长。
- 完整字幕意群与完整视频论述分别审核；有影响判断的数字/否定/实体识别歧义须听辨或更换素材，不能删去必要解释以躲过ASR问题。
- 旧选段缓存因策略版本及全文指纹失配自动失效；保留CPU离线ASR原始证据，不调用识别API、不用large-v3回退。
- 模型仍返回短句时，只能从程序计算的真实连续长上下文候选中重新选择；仍须独立审核完整论述，不能自动延长后直接发布。文案读取整段内容，不能只用前20条字幕。
- 日常识别配置统一读取`asr_production_config.json`。Qwen CPU识别与逐字对齐分别运行并验证同一份音频、完整核心区、权重版本及字符守恒；本素材CPU重做使用真实任务34035892469的原始证据，不把OCR原片错字视为正确答案。
- 投稿前检查实际文件时长、源区间、元数据与观点审核证明，旧短片无论从日常、指定批次或旧库存入口进入都被拦截。
- 单片有界超时并隔离，合格同批片保留。真人动态至少5/6，audio_card最多1/6，人物/黑边/水印/二维码/字幕/去重门禁不放宽。
- 日常生产工作流从main运行。FC包包含相同策略模块；部署核对index和策略模块哈希，发布/生产定时器必须使用LATEST。
- 今日隐藏短片的重做配额仅针对逐条验收的新六条；历史回执保留，明日起恢复正常每天六条。已隐藏成片不恢复公开、不重复上传。
