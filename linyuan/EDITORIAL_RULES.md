# 日常完整观点标准（2026-09-09 更新）

日常每天最多3条，北京时间10:00、16:00、21:00；周日21:00保留给完整访谈。以2～3分钟为主，可更长，拒绝十几秒短片。合格库存不足不凑数。

- 日常选段与投稿共用 `editorial_policy.py`，版本2026090604，实际MP4下限120秒，主目标180秒；不机械裁掉必要解释。
- 每条保留一个连续论述：开头独立可懂，观点、理由或案例、结尾齐全。不能拼接无关短句、补空白、变速或重复来凑时长。
- 用户已取消连续选段的强制模型“观点完整性”审核。生产和FC均记录status=skipped及策略版本2026090901，不伪造模型通过证明。字幕意群、残句与已确认ASR污染检查保留；剪除中间插语仍须原有语义复核。
- 旧选段缓存因策略版本及全文指纹失配自动失效；保留CPU离线ASR原始证据，不调用识别API、不用large-v3回退。
- 模型仍返回短句时，只能从程序计算的真实连续长上下文候选中重新选择；按完整问答边界选段，不拼接无关问题凑时长。文案读取整段内容，不能只用前20条字幕。
- 日常识别配置统一读取`asr_production_config.json`。Qwen CPU识别与逐字对齐分别运行并验证同一份音频、完整核心区、权重版本及字符守恒；本素材CPU重做使用真实任务34035892469的原始证据，不把OCR原片错字视为正确答案。
- 投稿前检查实际文件时长、源区间、元数据与明确的审核状态，旧短片无论从日常、指定批次或旧库存入口进入都被拦截。
- 单片有界超时并隔离，合格同批片保留。每天3条上限下均使用真人动态以满足至少70%的比例，人物/黑边/水印/二维码/字幕/去重门禁保留。
- 日常生产工作流从main运行。FC包包含相同策略模块；部署核对index和策略模块哈希，发布/生产定时器必须使用LATEST。
- 历史回执保留；已隐藏成片不恢复公开、不重复上传。

## 字幕阅读标准（2026-09-10 用户确认，所有后续出片默认启用）

基准为用户认可的《林园-字幕修复版.mp4》（6分04秒，来源 BV16QYK6qEwm）。

- 先做展示层精简，再按完整意群分屏：去明确的口头重复和起句语气词；原始ASR、原音、删改时间和前后文字全部留存。禁止凭语义猜测错词；否定、条件、数字、专名及有意义的强调不得被简化成另一观点。
- 每屏一至两行，目标2～5秒、优先不超过6秒；确需保留完整慢句时允许至8秒；保留原话时间，不变速、不为追求短字幕而切开“70%来自老行业”“跟银行借贷去分红”等结构。
- 白底深色大字、无厚描边；720×1280人物动态窗口保留既有位置，字幕可见字高约44px（Noto CJK的ASS字号约64，不能把ASS字号误当实际字高）。横屏、竖屏、方屏均使用浅色字幕底板，保留原画比例。
- 顶部使用完整短标题，最多两行，不再把“有的甚至……”等口头残句挂满屏；末尾不留逗号/句号，问号/感叹号可保留。
- 每条记录readability_version=2026091001和*.editing.json；旧分屏缓存不复用，原始转写仍可复用。此版本不改变FC元数据协议，不影响既有库存的兼容读取。
