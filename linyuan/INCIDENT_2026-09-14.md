# 2026-09-14 最近20次出片失败核查

本次逐一读取 GitHub Actions 最近20次 failure 的完整 produce job 日志。范围按运行创建时间排序：#870–#890，排除非失败的 #888。这些任务全部在投稿前失败；另核对 FC 发布日志，不能将 Actions 成功等同于 B站已发布。

## 今日实际发布与故障

- 北京时间10:05:04，FC记录成功投稿 BV17BYr6PE8d（预制菜）。这是投稿回执，不代替平台公开状态核验。
- 14:00调用取 artifact 10324914366，两次120秒后保留字节分别为183861248、338870272，最终返回 `published=0, artifact_download_retryable=1`。
- 原代码随后删除临时目录/未完成包；下次从零下载。已增加有界续传预算，并按不可变artifact ID保留跨调用检查点；无效ZIP、CRC和不支持Range仍重新下载。
- 当日计数为3、时点为`[1,1,10]`时，旧补发公式把14点缺口抵掉。新公式使用实际缺失时段，在每日4条上限内补最早的适配时段，保留旧状态兼容和防重。

## 逐次核查

|运行|失败阶段|日志证据与处理|
|---|---|---|
|#890|人物身份|仅1/6帧匹配；不放宽身份门禁，换可确认素材|
|#889|取景预检|原画字幕裁切不可行，猜测窗口仍有角标；预检现复用带角标避让及最小尺寸约束的取景计算，恢复实测|
|#887|识别漏检|1421秒仅3字，极短时间戳算出37.5字/秒，绕过双低阈值；增加长音频灾难性稀疏检查，不再误记为终局无合格选段。原音频前30秒存在显著50Hz成分；未声称修好原音频或成功转写|
|#886|取景预检|全部选段/完整版无法清理原画；同上通用修复，仍须真实画面验收|
|#885|取景+模型|与#880相同母片：600秒超时、缺脸、小窗口、原画预检失败；保留转写，有界恢复，不能重复执行就宣称解决|
|#884|取景预检|取源已恢复，但两个选段原画清理未过；不归为下载故障|
|#883|裁切尺寸|292×218及208×154窗口放大超2倍；源帧为双人分屏，修复先保留更宽原画，再执行原有身份/清晰度/动作门禁|
|#882|黑区|一段持续黑区超裁边范围，另一段成片黑边；保持拒绝，不为凑量放行|
|#881|源分辨率|最高480×360，短边低于当前480标准；更换高清源|
|#880|取景+模型|600秒文本推理超时，随后三段取景拒绝；重试#885复现同一问题|
|#879|裁切尺寸|284×210与278×206窗口；与#883同一取景尺寸计算缺陷|
|#878|角标避让|两个选段无法避开来源标记同时保全脸；保留限制，须实际检查新取景结果|
|#877|取源|总预算3600秒耗尽；#884同源后来下载成功，已有母片检查点可复用|
|#876|人物身份|0/6帧确认，无可确认人脸；不自动认定林园|
|#875|取景预检|两个选段与完整版原画清理失败；与#889同类|
|#874|角标避让|无法避开角标同时保全脸；不放宽角标限制|
|#873|人物身份|0/6帧匹配；保持拦截|
|#872|选段|141秒被模型拆16个主题，0/4候选；增加过度逐句拆分识别及一次不复用缓存的纠正，仍失败归可恢复故障|
|#871|选段|165秒拆24个主题，0/11候选；同上|
|#870|选段|250秒拆34个主题，0/39候选；真实输出含“继续讲”“解释为何”等目录，加入真实回归fixture，纠正话题图，不能将此当终局淘汰证据|

## 验证与边界

下载/补发相关86项本地回归通过。取景、识别、话题图57项通过；预检/身份/原画门禁116项通过、1项依赖环境跳过。测试有重叠，不相加充当独立用例数量。

FC修复提交7372a966；已触发自动部署，最终状态需按部署代码读回核验。工作流 `linyuan-incident-0914.yml` 在回归成功后恢复#883、#889、#870；恢复登记、开始运行均不是成片或投稿成功。原身份、动作、水印、120秒时长、标题与防重检查继续有效。各次恢复的实际结果应另记，不能将20次失败都称为20个已修复bug。

追加：生产前完整检查发现原异常格式用例不应增加重试，已将纠正限定于逐句话题图；同时将selector缓存版本提升为15，避免复用旧空选段。第一轮恢复#892–#894在前置回归处停止、未进入出片；修正后使用repair0914v2重新恢复。综合回归194项通过、1项环境跳过。

## 线上验证追加（北京时间15:52）

- 标题审核回退没有把失败检查项传回结构化拟稿，真实横屏部署检查169因此连续三次漏限定。修复将检查项与可执行要求传回，但不把错误标题或主持人假设作为事实；同义表达和完整问句按实际含义审核。34项标题回归通过。
- 提交4e1922e7对应部署34818195132，真实CPU横屏/竖屏、证据合并、生产部署均成功。15:46:39代码读回`verification_complete=true`，代码SHA256为051cfbf0478d2c98f90eb77237d7ed007d42b0165f06a4d0e39c36668057f030。部署回归274项通过。
- 库存补发任务34819457082在15:52返回`published=1`，新稿BV1kSYk6yEx7《林园：投资方向正确是投资原则第一重要》，补记14点横屏时段。当天count=4，时段记录[1,1,10,14]。创作中心当时为state=-30、审核中；此工作流因尚未公开退出1，不能当作投稿失败再次上传。
- #895（原#883）越过小窗口报错，但4147/6206帧均未达到动态目标人脸标准；源图YuNet人脸约59–79px宽，仍低于既有96px短边要求。没有降低标准或把小脸认作已核验真人成片。
- #896（原#889）仍未通过角标预检。实看源片确认片头为带标记提问卡，前两个取景采样没有真人，回落猜测窗口保留右上角标；不能宣称修复成功。
- #897（原#870）仍返回逐句话题图，但现归可恢复模型故障，未写终局淘汰。进一步查到原话“继续给我们讲讲……”不在原文提问规则中；提交7c7eda00补此明确请求句式，真实字幕直接选出1–77条、242.76秒原始连续回答，无伪造完整性审核。相关33项回归通过；repair0914v3正在实际恢复。

## 发布最终核验（北京时间15:53）

每日实际发布验收[34819980404](https://github.com/fastisrealslow/bilingual-subtitle-burner/actions/runs/34819980404)成功。创作中心已确认以下四条全部开放浏览；独立文件SHA256均不同，无缺失文件指纹。原始只读核验结果保存在`INCIDENT_2026-09-14_publication.json`。

|实际投稿时间|稿件|状态|
|---|---|---|
|01:10|[BV11qYB6MEyq](https://www.bilibili.com/video/BV11qYB6MEyq)|公开|
|01:21|[BV11BYB6iEHY](https://www.bilibili.com/video/BV11BYB6iEHY)|公开|
|10:04|[BV17BYr6PE8d](https://www.bilibili.com/video/BV17BYr6PE8d)|公开|
|15:51（补14点横屏）|[BV1kSYk6yEx7](https://www.bilibili.com/video/BV1kSYk6yEx7)|公开，127秒|

每日4条上限已经达到，因此今天16点/21点不会再新增投稿；不能把“今日四条已公开”表述为四个计划时段都按时完成。

## 最后一条恢复结果

#898 / [34820027061](https://github.com/fastisrealslow/bilingual-subtitle-burner/actions/runs/34820027061)在15:57:28实跑确认“直接保留1组原始提问及连续回答，跳过模型选段”，并按用户设置跳过模型完整性审核。原#870选段错误已不再复现。15:58:33随后由画面门禁拒绝：来源角标无法避开且保留完整人脸。没有生成合格入库片或触发新投稿，需换可用画面来源。

本次结论：最近20次失败均已逐条核查；确认的下载/补发/选段/识别漏检/取景尺寸/标题重试程序问题已提交并验证。FC生产修复已读回确认，14点漏发已由合格库存补齐且公开。当日四条均公开，但其中两条在凌晨；当天后续时段受自然日四条上限约束。三条素材恢复均明确停在实际画面标准，不将失败素材或尚未通过的成片算作恢复成功。
