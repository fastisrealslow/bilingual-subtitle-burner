# 日常生产前置回归阻塞修复

用户已停止 source100 统计。本次仅修复正常生产阻塞，不启动固定100批测、FC部署或投稿。

## 已确认根因

- 失败证据：生产 run 35314064950、35313112450，均在“CPU离线ASR与完整观点规则回归”停止，尚未进入实产。
- 两个失败测试：`test_selector_exposes_later_complete_answers_without_borrowing_time`、`test_automatic_pipeline_tries_seventh_answer_after_six_picture_failures`。
- 选择器 v26 为同主题的完整145秒问答继续生成295秒重叠合并候选，8个独立问答变为15个候选，改变后续逐条画面失败时的尝试顺序。
- 先前519项版式回归没有覆盖这两个测试，不能证明主生产前置测试正常。

## 修改

- 选择器 v27：已有自然问答边界且本段达到120秒时，保留独立候选，不再因后续问题共有行业词而叠加合并；短问答仍按原有同主题、转题边界、完整回答和330秒上限检查。
- 不改失败用例的期望，不取消测试，不放松出片/质量门禁。
- 将主生产启动前全部21个测试文件纳入自动CPU版式回归。文件清单已核对，无遗漏。

## 验证状态

提交前已完成语法、diff及测试清单核对。本地依赖下载超时，实际回归以本提交触发的 GitHub Actions CPU 结果为准；不能先记为通过。
通过前置回归只表示该阻塞解除，恢复真实成片及平台公开仍须分别核验。未把库存或投稿恢复写为完成。

## Actions 实测结果

- 修复提交：`c5dbe7752478e09c813c69fa40405795fa36779a`。
- CPU 回归：[run 35329045077](https://github.com/fastisrealslow/bilingual-subtitle-burner/actions/runs/35329045077)，success；631 passed, 1 skipped, 1 deselected，23.32秒。
- 两项原失败用例未修改断言且已包含在通过的测试组内；主生产前置21个测试文件全部包含。
- 本提交触发记录只有CPU回归与既有Pages更新，无FC部署或生产/投稿任务。
- 状态：前置回归阻塞已解除；修复后的真实成片、公开投稿仍待独立核验。
